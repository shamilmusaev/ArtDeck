#!/bin/sh
# Block commits that leak secrets or personal data. Enable once with:
#   git config core.hooksPath .githooks
python scripts/check_secrets.py || {
  echo "pre-commit: secret/PII scan failed (see above). Commit aborted." >&2
  exit 1
}
