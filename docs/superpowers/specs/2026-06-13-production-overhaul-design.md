# Steam Art — доведение до production + новые фичи

**Дата:** 2026-06-13
**Ветка:** `feature/production-overhaul`
**Статус:** утверждён, готов к плану реализации

## Цель

Превратить утилиту дозаливки артов для non-Steam игр в полноценное production-приложение:
рефакторинг движка на модули, 7 новых/улучшенных функций, редизайн UI, автотесты,
сборка в .exe и обновлённые доки. Движок остаётся stdlib-only; CLI не ломается.

## Решения по scope (зафиксированы)

- **Охват Steam-игр:** только **установленные** (локально, оффлайн, без Steam Web API-ключа).
- **Структура списка:** **две вкладки** сверху сайдбара — `Non-Steam | Установленные`.
- **Production включает:** сборку в .exe + автотесты + обработку краевых случаев + обновление доков.
- **Подход к коду:** рефакторинг движка на пакет `steam/` (вариант A).
- **i18n:** `web/i18n.js`, словари ru/en, переключатель в шапке, выбор в `localStorage`.

## Архитектура

`steam_art.py` разбивается на пакет `steam/`. Публичные имена сохраняются, CLI и сервер
продолжают работать. `steam_art.py` остаётся тонкой CLI-обёрткой; `steam_art_app.py`
меняет только импорт на `import steam as engine`.

| Модуль | Ответственность |
|---|---|
| `steam/paths.py` | `find_steam_path`, `list_accounts`, `account_paths`, библиотеки (`libraryfolders.vdf`) |
| `steam/users.py` | **NEW** — имена аккаунтов (`loginusers.vdf`) + аватары (`avatarcache/`); маппинг uid↔SteamID64 |
| `steam/vdf.py` | низкоуровневый `parse_binary_vdf` + текстовый VDF-парсер (`.acf`/`loginusers`) |
| `steam/library.py` | `load_shortcuts` (non-Steam) + **NEW** `load_installed` (`appmanifest_*.acf`); единый формат игры |
| `steam/sgdb.py` | `api_get`, `search_games`, `list_arts` (+ фильтр `animated`), `fetch_art_url`, классы ошибок |
| `steam/arts.py` | `apply_art`, `art_status`, `existing_art`, `find_orphans`, `clean_orphans` |
| `steam/__init__.py` | реэкспорт публичного API → `import steam as engine` работает как раньше |

Каждый модуль stdlib-only и тестируется изолированно.

### Единый формат игры

```
{appid, name, kind: "shortcut" | "steam", icon, status}
```

`status` — `{art_type: bool}` по `ART_TYPES`, как сейчас.

## Фичи

### 1. Вкладка «Установленные» (Steam-игры)
`load_installed(steam_path)` читает `libraryfolders.vdf` → все библиотеки →
`steamapps/appmanifest_*.acf` (текстовый VDF: `appid`, `name`, `installdir`). Обе категории
игр приводятся к единому формату. Переключатель вкладок в сайдбаре. Применение арта идентично —
те же `grid/<appid><suffix>`. Очистка осиротевших по-прежнему трогает только appid ≥ `NONSTEAM_MIN`,
официальные арты Steam-игр в безопасности.

**Фильтр не-игр (важно):** `appmanifest_*.acf` содержит не только игры — Steamworks Common
Redistributables (228980), Proton (различные appid), dedicated-server-тулзы и пр. Отсекаем по
набору известных tool-appid + эвристике (имена «Steamworks Common Redistributables»,
«Proton …», «… Dedicated Server», «Steam Linux Runtime»). Список tool-appid держим в
`steam/library.py` как константу; неизвестное оставляем (лучше показать лишнее, чем спрятать игру).

### 2. Анимированные обложки
`list_arts` получает параметр `animated`: при фильтре запрашивается `types=animated` (либо
`animated,static`). SGDB отдаёт анимацию для grids/heroes/logos (для icon — нет). На фронте —
чекбокс «Только анимированные» рядом с вкладками типов. Карточки с анимацией помечаются бейджем ▶.
Применение — как у статики; **подтверждено**: Steam воспроизводит кастомные анимированные обложки
формата **APNG / animated WebP** из `grid/<appid><suffix>`.

**Производительность:** APNG/WebP-анимации бывают очень тяжёлыми. В сетке карточек грузим только
`thumb` (статичное превью), а полную анимацию подгружаем лениво — в лайтбоксе предпросмотра
(фича №7) или по наведению, чтобы не было фриза при открытии вкладки.

### 3. Имена и аватары аккаунтов
`steam/users.py`: uid (account ID) → SteamID64 = `uid + 0x0110000100000000` (константу в коде
писать полными 16 hex-цифрами, чтобы не опечататься). Имя из `Steam/config/loginusers.vdf`
(`PersonaName`), аватар локально из `Steam/config/avatarcache/<steamid64>.png` (если есть).
`/api/state` отдаёт `[{uid, name, avatar_url}]`; новый эндпоинт `/api/avatar?account=` стримит
локальный файл.

**UI (важно):** нативный `<select>` не умеет рендерить картинки, поэтому селект аккаунта
заменяется на **кастомный дропдаун** — кнопка с аватаром + именем, раскрывающийся div-список
строк (аватар + имя + uid). Fallback: нет имени → uid, нет аватара → инициал/заглушка.

### 4. Редизайн UI в духе steamgriddb.com (через `/frontend-design`)
Не просто починка, а **переработка визуального языка** под эстетику steamgriddb.com:
- тёмная навигация сверху, контентная область с крупной сеткой арт-карточек;
- ряд вкладок типов артов (Grids/Covers · Heroes · Logos · Icons) в стиле SGDB;
- **панель фильтров** над сеткой: Style, Dimensions, тип (Static/Animated) — как на сайте;
- карточки с hover-оверлеем (размеры, стиль, действие), бейджи размера/стиля/анимации;
- фирменная палитра SGDB (тёмный фон + акцент), аккуратная типографика, плотная сетка.

Реализуется скиллом frontend-design. Попутно чиним известные грабли вёрстки из CLAUDE.md
(аспекты карточек `position:absolute` в `.imgwrap`, `grid-auto-rows:max-content`, `min-height:0`,
загрузка без `loading="lazy"`, shimmer по карточкам). Прежний язык «стекло+неон» заменяется,
поэтому правки в текущем `web/style.css` (uncommitted на старте ветки) пересматриваются.

### 5. Английский язык
`web/i18n.js`: словари `ru`/`en`, функция `t(key)`, переключатель RU/EN в шапке, выбор в
`localStorage`. Все строки UI выносятся в словари. **Контракт ошибок:** бэкенд возвращает
короткие **коды** (`no-key`, `network`, `not-found`, `bad-id`, `sgdb`), а не готовый текст;
фронт переводит код через `t()`. Русские строки исключений движка (`sgdb.py`) остаются только
для CLI/логов и в UI напрямую не показываются.

### 6. Иконки игр в списке
- non-Steam: поле `icon` из `shortcuts.vdf` (путь к локальному .ico/.png).
- Steam: **multi-location lookup** (пути менялись между версиями клиента) — пробуем по очереди:
  1. `appcache/librarycache/<appid>_icon.jpg`
  2. `appcache/librarycache/<appid>/*` (glob — новая структура с подпапкой на appid)
  3. нет файла → заглушка.

Новый эндпоинт `/api/gameicon?account=&appid=` стримит найденный локальный файл; в списке —
миниатюра слева от названия, fallback на иконку-заглушку. Это самый хрупкий пункт (разные места
хранения) — при отсутствии файла просто показываем заглушку, не падаем. Из-за роста списка
(установленных игр могут быть сотни) — **дебаунс** фильтра и ленивая подгрузка иконок.

### 7. Крупный предпросмотр перед установкой
Клик по карточке (или по кнопке-лупе рядом с «Установить») открывает лайтбокс с полноразмерным
артом (`a.url`), размерами/стилем и кнопкой «Установить» внутри. Закрытие по Esc/клику на фон.

## Production

### Автотесты (`tests/`, stdlib `unittest`, оффлайн)
- `test_vdf.py` — бинарный и текстовый VDF на фикстурах (мини `shortcuts.vdf`, `appmanifest`, `loginusers`).
- `test_library.py` — `load_shortcuts` + `load_installed` на временных папках-фикстурах.
- `test_arts.py` — `apply_art`/`existing_art`/`find_orphans` во `tmp`.
- `test_users.py` — uid↔SteamID64, парсинг имени.
- `test_api.py` — `Server` в треде, дёргаем `/api/...` с замоканным движком (monkeypatch SGDB), без сети.

SGDB-сеть мокается — тесты детерминированы и оффлайн.

### Краевые случаи
Нет Steam / нет ключа / нет сети / пустые списки / битый VDF / Steam запущен / нет аватара/иконки —
аккуратные состояния в UI (тосты, заглушки), без падений.

### Сборка в .exe
`build.py` + `run_build.bat`: PyInstaller, один `--onefile --windowed` exe из `steam_art_app.py`.
`web/` в бандле (`--add-data`), путь к ресурсам через `sys._MEIPASS`. Иконка приложения.
`steam_art.key` НЕ бандлится (создаётся рядом с exe при первом вводе ключа). PyInstaller —
dev-зависимость, в движок не тянется.

**Известный риск:** `--onefile --windowed` с pywebview требует на машине рантайм
**Edge WebView2** (на Win11 есть из коробки) и аккуратной упаковки `pythonnet/clr` (возможны
hidden-imports / hooks PyInstaller). Сборку проверяем запуском exe на чистом профиле; в README —
строка про требование WebView2. Если упаковка pythonnet окажется проблемной — фолбэк на
`--onedir` вместо `--onefile`.

### Доки
Обновить `README.md` (новые фичи, сборка exe, запуск тестов) и `CLAUDE.md` (модульная структура,
источники данных, i18n-грабли).

## Порядок реализации

1. Рефакторинг движка на пакет `steam/` + тесты (зелёные) — фундамент.
2. Бэкенд: installed games, users/avatars, icons, animated, новые эндпоинты + тесты.
3. Фронтенд: вкладки, фильтр анимации, аватары, иконки, предпросмотр, i18n.
4. Ревью UI через `/frontend-design`, починка.
5. Сборка exe + финальные доки.

## Брендинг и юридическое

- **Убрать упоминания Hydra Launcher из публичных материалов** и переформулировать «зачем нужен
  инструмент» универсально (любой non-Steam ярлык: ручное добавление, эмуляторы, сторонние
  лаунчеры). Затрагивает: `steam_art.py` docstring (делается в Плане 1), `README.md` (План 4).
  `CLAUDE.md` — внутренний гайд, можно оставить, но тоже причешем при обновлении доков.
- **Дисклеймер** «Не аффилировано с Valve или SteamGridDB» — в README, в шапке/футере UI и в
  docstring CLI.
- **Торговая марка Steam (Valve):** для OSS-pet-проекта обычно некритично, но фиксируем как риск.
  Решение про имя продукта и логотип-«S» зависит от намерения распространения (см. открытый вопрос
  ниже). Минимум — дисклеймер; при коммерческом векторе — переименование без слова «Steam» в
  названии/домене/логотипе и редизайн логотипа.

### Открытый вопрос (решить до Плана 3/4)
Коммерческое распространение или OSS-pet-проект? От этого зависит: оставляем ли «Steam Art» +
логотип-«S» (OSS) или переименовываем и делаем новый логотип (коммерция). Дисклеймер добавляется
в любом случае.

## Вне scope (YAGNI)

- Steam Web API / неустановленные купленные игры.
- Запись в `shortcuts.vdf` (только чтение — безопасно при запущенном Steam).
- Кроссплатформенность за пределами Windows (целевая ОС — Windows).
